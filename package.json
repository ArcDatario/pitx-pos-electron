{
  "name": "pitx-pos-transfer",
  "productName": "PITX POS Transfer",
  "version": "1.0.4",
  "description": "PITX POS -> TSMS transfer & submission desktop app",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "dev": "tsc -p tsconfig.json --watch",
    "dist": "npm run build && electron-builder --win"
  },
  "author": "PITX",
  "license": "UNLICENSED",
  "dependencies": {
    "axios": "^1.7.7",
    "bootstrap": "^5.3.8",
    "electron-updater": "^6.8.9",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.5",
    "@types/node": "^20.14.9",
    "electron": "^32.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.5.4"
  },
  "build": {
    "appId": "ph.pitx.pos.transfer",
    "productName": "PITX POS Transfer",
    "icon": "build/logo.ico",
    "directories": {
      "output": "release-fixed"
    },
    "files": [
      "dist/**/*",
      "src/renderer/**/*",
      "config.default.json",
      "build/logo.ico"
    ],
    "win": {
      "target": "nsis",
      "signAndEditExecutable": false
    },
    "publish": {
      "provider": "github",
      "owner": "ArcDatario",
      "repo": "pitx-pos-electron",
      "releaseType": "release"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
